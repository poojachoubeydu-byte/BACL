# Example pipeline root

Minimal fixture directory used by `bcal audit --path examples/pipeline_root`
and by the `.github/workflows/bcal_audit.yml` workflow on release.

Replace with real pipeline outputs when wiring BCAL into your own pipeline.
